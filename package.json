{
  "name": "server",
  "version": "1.0.0",
  "main": "server/index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon .",
    "dev": "cd client && npm run build && cd .. && nodemon .",
    "setup": "npm install && cd client && npm run build && cd .. && nodemon .",
    "heroku-postbuild": "cd client && npm install && npm install --dev && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.2",
    "nodemon": "^2.0.7"
  },
  "optionalDependencies": {
    "fsevents": "^2.3.2"
  },
  "engines": {
    "node": "16.x",
    "npm": "7.x"
  }
}
